# handoff 2026-09-01

Closed bbk-rar.2 (876c6aa) — VStack and HStack. Next is bbk-rar.3, Page, which
this was blocking; bbk-0kt.1 (the filtering functions) is the alternative if a
pure-logic bead suits better.

The one judgement call: HStack aligns centre rather than stretch. A stretched
row makes a badge the height of the paragraph beside it, and the plan's prop
table doesn't say which default. Still slightly tempted by `stretch` for
symmetry with VStack — resisted it because symmetry isn't what a caller wants
here, and Rule 1 says the obvious thing should need no configuration.

Left alone deliberately: the `gap` scale stops at 16 and skips 7, 9, 11. It is a
guess at what a call site will want, not a rule from the plan, and widening it
later is free. Also no `wrap` prop on HStack — the plan lists three props, and
I'd rather see a real overflowing row first.

Screenshots for the review went to the scratchpad rather than harness/shots/,
since that directory is untracked; noted on bbk-m4b, which the design reviewer
independently walked into.
