# handoff 2026-09-03

Step 3 and step 4 are both closed — filtering logic, FilterBar, the post
examples and /kitchen-sink (bbk-0kt, bbk-rar). The interesting part is the bug
reviewer-correctness caught in bbk-0kt.2: the tag row was counted from the raw
query while the list ran on the delayed one, so tags greyed out a keystroke
early. useFilteredItems now returns { items, criteria }; plan §3.5 amended.

Left undone on purpose: the dev server. Port 3000 is held by another chat's
server for this same repo, and the fix — autoPort in .claude/launch.json — was
interrupted rather than applied. Whoever picks it up should decide whether this
project ever needs 3000 specifically before flipping it.

Mid-thought and unresolved: in the cards grid, a post with no cover stretches to
the row height and shows a tall blank body above its tags (visible in the
kitchen-sink capture). It is honest grid behaviour and I left it, but it is the
kind of thing step 4.5 exists for — bbk-icl is where it belongs if it bothers
anyone looking at it. I did not file it as a bead because I am not sure it is
wrong, only that it is noticeable.

Next: step 4.5 (bbk-icl) is where the looking-and-fixing beads have piled up —
bbk-box, bbk-u5p, bbk-2vw and bbk-ge7 all want the same pass over the same
screens, so they are cheaper together than one at a time.
