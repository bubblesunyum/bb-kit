# handoff 2026-09-01

Shipped Skeleton, Separator and EmptyState — bbk-rar.5 in f754a5a plus bbk-8ua centering fix in 7014baf. All three are server-renderable, storybook-visible, registry-built, and taste/correctness/design reviewed (12 captures in /tmp/bbk-*.png).

Was mid-thought on the Skeleton lines API: `width` on a multi-line skeleton sizes the container, not each line, and `height` sizes each line — the right defaults for text, but an image placeholder wants a single square so callers will use width+height on a single line. That split is documented but will surprise someone expecting width to trim each line.

Design review caught EmptyState left-aligned when wrapping — H3/Text default `align="start"` was beating parent `text-center`. Fixed with `align="center"` on both. The other design nit (5 stories not captured) and correctness lows (EmptyState H3 heading-order, data-slot override, Skeleton style vs class) left as follow-ups, not blocking.

Next: bbk-rar.6 Button (Primary/Ghost/Link/Danger, variant default alias, focus-visible) is unblocked by this, and bbk-0kt.1 filtering functions is parallel P1 no-UI work. Also bbk-3cy to route Text/Stack through classFor.
