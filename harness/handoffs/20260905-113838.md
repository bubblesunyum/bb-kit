# handoff 2026-09-05

Read bbk-ejv before touching clash's colours. The a11y sweep (bbk-dv5.1) proved
clash light has no danger that is both a strong red and 4.5 as text, so the
shipped #F87C89 is a compromise — a rose, not a warning. Owner's call at 4.5.

The thing I keep circling: §4.6 gives `danger` a 3.0 floor because it was
written for fills, but `List`'s error row and `Text tone="danger"` paint it as
text at 4.5. That mismatch is what let bbk-abw's first fix (#F04C57) pass every
test and still be wrong. I did not add a `["danger", "card", 4.5]` row to
CHECKS, because the plan says thresholds never move and §4.6 line 432 explicitly
scopes "Text on a highlight" to the role rather than the plain one — so the same
reading probably scopes danger too. Tempting, still think it's the right shape,
still think it's the owner's edit rather than mine.

Also unresolved and unfiled: the four-up cell selector and COMBOS order in
scripts/a11y.mjs both assume the decorator's shape and iteration order. Both
hold today; neither is checked. reviewer-correctness flagged them as assumptions
rather than defects and I agreed, but a third palette would silently mislabel
every violation.

Next I'd pick up bbk-2hb — SearchField inside a real <form>, cheap and it is the
last untested interaction in v0.
