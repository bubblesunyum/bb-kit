# handoff 2026-09-06

bbk-jtr closed on CI run 34067371817 (first green, commit 07f6fca). Next: the step-4.5 cluster, bbk-box first.

If settle-pseudo starts timing out on every story, Storybook probably inlined all CSS into <style> — the external-sheet signal it waits on would be gone. It fails loud, not blind; adapt the signal, don't lengthen the timeout.
