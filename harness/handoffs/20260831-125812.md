# handoff 2026-08-31

Closed bbk-bp6.3 — the shadow split works, verified through the utility rather
than the variable, which was the part I didn't trust.

The real find was accidental: storybook was importing tokens.css alone, so every
story had the colour variables and no tailwind whatsoever. Utility classes just
did nothing, silently, and nothing would have caught it because the only story
that existed used inline styles. If step 4 had started before this, the first
component would have looked broken for a reason nobody would have guessed at.
Remembered as storybook-needs-globals-css.

Still nagging: bbk-780. Clash dark puts 40-60% black on a near-white page and it
reads heavy — not wrong per §5.6, but it looks wrong, and I keep wanting to key
the shadow colour to the page's lightness instead of the mode. Didn't, because
that's a plan change and there's no real card to judge it against yet. Worth
deciding once Surface exists rather than carrying it further.

Next: step 2's three remaining P0s, in ledger order — bbk-bp6.4 (ThemeProvider),
bbk-bp6.5 (the nested Theme wrapper), bbk-bp6.6 (the §4.6 check function). The
check function is the one with teeth; bbk-h5l and bbk-c0j are already-known
failures waiting for it.

Also still true: reviewer-design can't run, because the packet has no way to
carry screenshots (bbk-m4b). I've been reviewing screens by eye instead.
