# handoff

Setup session — no kit code exists yet. The harness is installed and green, and
r7 is filed as 51 beads (bbk-5e9 through bbk-e2x) chained in the plan's order.
Next: bbk-5e9.1, check whether shadcn's registry template still does what §9
Step 0 expects, because everything else in Step 0 forks on that answer.

Two things I'd want the next session to know before it trusts anything here.

The ledger's dependency chain encodes the plan's order, which r7 says twice not
to reorder. That makes `bd ready` the authority on what to do next — but it also
means a wrong edge is invisible, since a blocked bead just doesn't appear. If
something you expect to be claimable isn't, check the edge before assuming the
plan meant it.

I compressed r7 into CLAUDE.md and I'm not fully confident the compression is
lossless. The audit (see the commit `act on the session audit`) caught four
places where I'd stated something the plan doesn't say, or welded two of its
arguments into one that didn't follow. I fixed those. I don't think I found all
of them. Where CLAUDE.md and the plan disagree, the plan wins — that line is in
CLAUDE.md for a reason, and it is not a formality.

bbk-ifq blocks the filtering work and needs the owner, not an agent: r7
contradicts itself on whether filterItems returns a plain or readonly array, and
Rule 4 points at §3.8 for the prop table that lives in §3.5.

Unresolved and nagging: the four-up Storybook decorator (bbk-bp6.10) is the only
thing that makes the clashing palette do its job, and it's filed as a normal
task under Step 2. It's really a precondition for reviewing any component at
all. If component work starts before it exists, every story gets reviewed in one
palette and the second one becomes decoration.
