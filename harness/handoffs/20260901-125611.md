# handoff 2026-09-01

Closed bbk-bp6 — step 2 is done, step 4 (components) is unblocked, bbk-rar.1 is next.
The correctness reviewer claimed nested mode-only overrides are silently broken by a
Lightning CSS polyfill of light-dark(). It is wrong — measured it: zero lightningcss
rules in the sheet, --page stays unresolved, the mode-only island computes #E7EDEA
inside a dark page. Don't re-litigate it; the four-up Nesting story is the standing proof.

The find worth keeping is bbk-bp6.5's: Theme now paints whenever given EITHER palette
or mode, not just mode as plan §4.3 words it. A palette-only island stayed transparent
and its text flipped to clash's near-white over forest's pale background — the same
half-and-half island the dark: ban exists to prevent, reached by another route. Written
up as [theme-paints-on-either-prop]. §4.3's wording is now wrong; I left the plan alone
rather than editing the spec mid-step, but someone should decide whether to amend it.

Was mid-thought on whether /tokens and Tokens/Roles now overlap enough that one should
go. /tokens has the measured numbers and all four combinations on one scrollable page;
Tokens/Roles has the pseudo-states story and the toolbar. I think both earn their place
— different questions — but it nagged and I didn't resolve it. If Tokens/Roles ever
stops being where you check a forced hover, it has no reason to exist.

The thing that actually cost time: three separate ways a Storybook story silently
ignores the palette/mode toolbar, all of which present as "the toggle is broken". All
three are fixed and written up as [stories-must-not-pin-their-own-theme]. Read that
before adding any story in step 4, because the failure looks like a decorator bug and
isn't.

Also: the Browser pane cannot be screenshotted while hidden — captures come back solid
black with no error, which wasted a cycle. Use playwright. [screenshots-need-playwright-not-the-pane]

Colour work is deliberately parked in bbk-dv5, gated behind step 4 — you cannot judge a
retuned colour against a swatch grid, only against a real button and a focused field.
