# handoff 2026-08-31

Palette is finally visible outside Storybook — bbk-bp6.2, commit 00408cc.
Next is bbk-bp6.3 (shadows), the one §4.2 verification that still fails.
Skipped step 3 on the owner's call: filtering comes after components, not before.

Step 1 is closed but left two things on the floor deliberately. Looking at both
palettes in a real composition turned up two gaps in §4.6's *list of pairs* —
highlight-vs-muted and input-border-vs-muted, filed as bbk-c0j and bbk-h5l. The
owner deferred both as fine-tuning, so I removed the edges that had them
blocking bbk-bp6.6. Worth knowing before you write the check function: if you
add those two rows while implementing 6.6, two palette cells go red and you are
suddenly doing palette surgery nobody asked for. Add the rows when c0j/h5l are
actually picked up, not opportunistically.

The thing I could not pin down: highlight-vs-muted measures 1.023 in forest
light and *looks completely fine* on screen, because the two differ in hue and
barely at all in luminance. WCAG contrast only sees luminance. So the number is
right and my eye is wrong, and I do not have a good instinct yet for how many
other pairs in this palette are passing or failing on hue I cannot separate from
lightness by looking. If bbk-bp6.8 (/tokens with measured numbers) gets built,
that page is the place to find out.

Rejected and still tempting: making `--radius-*` four literals instead of a calc
chain off `--radius`. It reads cleaner and every value comes out identical. It
is wrong because retuning the base then silently moves three steps and leaves
`--radius-md` — the one shadcn components hardcode and the kit never writes —
sitting on Tailwind's stock default. The review caught this; I would have shipped it.

Note on running the app: another chat holds a Next dev server on port 3000 for
this same directory, and Next refuses a second one *per directory*, so autoPort
does not help. I verified against that existing server. Storybook on 6006 is free.
