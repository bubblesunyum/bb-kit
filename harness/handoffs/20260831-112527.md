# handoff 2026-08-31

Step 0 is scaffolded and the app builds; next is bbk-5e9.4, the registry files,
and I already did its research — `bd recall shadcn-registry-checked-2026-08` has
the exact item schema and build command, so that bead is transcription, not
discovery. Do it before Storybook: nothing yet exercises the registry at all.

The thing I couldn't settle: `shadcn init` installs `radix-ui`, `lucide-react`
and `tw-animate-css` beyond the plan's list. I kept them because globals.css
imports two of them at runtime now, and cutting back would break the
dropped-in-shadcn support §4.2 wants. Offered laurel the choice and wrapped up
before an answer, so it stands as my call, not theirs. If it bothers a later
reviewer, that's the reasoning to argue with.

Two things that will bite whoever is next:

- `.claude/launch.json` is gitignored as machine-local, so the `bb-kit` dev
  server entry I added is gone for you. Re-add it (npm run dev, port 3000) or
  `preview_start` has nothing to launch.
- The review packet was blind to `*.ts`/`*.tsx`/`*.css` until bbk-uw5 this
  session. Any review before commit 8bbb157 saw less than it claimed to. Worth
  distrusting if you're relying on one.

Not checked, and it nagged: whether `scripts/verify.sh` needs anything for
bbk-5e9.10. It already picked up `npm run build` on its own and prints `ok
build`, so that bead may be mostly done already — read it before starting it.
