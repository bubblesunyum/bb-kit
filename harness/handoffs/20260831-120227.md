# handoff 2026-08-31

Step 0 is done and committed (fa76a2c); epic bbk-5e9 closed 10/10, gate green.
The loose thread: src/tokens/tokens.css is generated and committed but imported
by nothing except the Storybook story, so the palette is real and invisible.

Picking up next: bbk-bp6.2 — import tokens.css, replace globals.css (still
shadcn init's output), then @theme inline, the dark: custom variant, @source,
the shadcn variable aliases and the border-color base rule. §4.2 lists five
verification steps and they are worth doing in order; the opacity-modifier one
(bg-primary/50 over a light-dark() value) is the combination I'd least expect
to just work.

I pulled Step 2's palette file forward to get there, which I went back and
forth on. Record<PaletteName, Palette> will not typecheck half-filled, so the
staleness gate (bbk-5e9.8) had nothing real to check without the values. The
alternative was typing it Partial<> so Step 0 could leave the data to Step 2 —
rejected because the optionality then ripples into every consumer of Palette
forever to buy a clean step boundary once. Still tempting, still wrong I think.
bbk-bp6.1 and bbk-bp6.2 both carry notes on exactly what landed early.

Two things I could not verify the way this project wants. Nothing writes
screenshots into the review packet (bbk-m4b), so reviewer-design never ran and
the Storybook work was checked by my own eye in the browser pane — the palettes
render in both modes and pseudo-focus-visible is really forced, but no second
opinion looked at it. And src/components does not exist yet, so the colour
scanner in bbk-5e9.7 is proven only against its own sample table. Its first real
test is the first component.

Watch the SCOPE glob in scripts/review.sh. It has now been silently too narrow
twice — bbk-uw5 for .ts/.tsx, then .mts this session — and both times the packet
looked complete. Check its file count against git status before spawning
reviewers.
