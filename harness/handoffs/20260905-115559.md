# handoff 2026-09-05

The ledger's standalone beads are gone — bbk-owj, bbk-2hb, bbk-tzf, bbk-m4b
closed in da5a0ef, all 18 left are epics or epic children. Branch is now `main`
(cabc9e1). **origin exists and is empty** (github.com/bubblesunyum/bb-kit), which
quietly unblocks bbk-714 and bbk-esu — both were parked on "no remote" and both
now just need doing; notes on each say what.

scripts/shoot.mjs is the one to know about. Every review packet before today said
"Captures: None" and anything on screen went to review unseen; now
`node scripts/shoot.mjs` with no argument shoots the stories whose files the
change touched, four-up so one PNG holds all four palettes. It reads
storybook-static, so `verify.sh --full` has to come first or you shoot the last
build — that is the trap, and it is silent.

Still tempted by a thing I did not do: shoot.mjs only knows Storybook, and the
/tokens page needs `next dev` to see. That felt like the wrong second capability
to build before anyone asked for it, but if a change ever lands in the token CSS
alone, the design reviewer has nothing to look at.

Would pick up next: bbk-esu, now that it can actually run — a workflow calling
verify.sh --full, with `npx playwright install --with-deps chromium` in it, since
a11y.mjs and shoot.mjs both need chromium. Then bbk-714, which is the same
decision wearing a URL. bbk-950 has a written recommendation on it and wants an
owner's yes rather than code.
