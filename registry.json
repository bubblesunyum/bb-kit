{
  "$schema": "https://ui.shadcn.com/schema/registry.json",
  "name": "bb-kit",
  "homepage": "https://bb-kit.local",
  "items": [
    {
      "name": "text",
      "type": "registry:ui",
      "title": "Text",
      "description": "Paragraph text and the wrappers that pick the element for you: H1-H4, Span, and Label. Size, tone, weight and align are props; tone names a role (default, quiet, primary, danger, on-primary, on-danger) rather than a colour, so it follows the palette and mode. Heading level and size are independent, so the semantically correct level never forces a look. No loading state - place a Skeleton yourself, at the height the real text will take.",
      "files": [
        {
          "path": "src/components/text.tsx",
          "type": "registry:ui",
          "target": "components/text.tsx"
        }
      ],
      "registryDependencies": []
    },
    {
      "name": "stack",
      "type": "registry:ui",
      "title": "Stack",
      "description": "VStack stacks children downward and HStack lays them out sideways, both with an 8px gap you never have to ask for. Gap takes Tailwind's own spacing scale rather than a second scale; align and justify are ordinary props, and HStack centres across the row because a row is nearly always something small beside a line of text. No loading state and no empty state - a stack takes arbitrary children, so it cannot tell empty from one child that renders nothing; place a Skeleton yourself.",
      "files": [
        {
          "path": "src/components/stack.tsx",
          "type": "registry:ui",
          "target": "components/stack.tsx"
        }
      ],
      "registryDependencies": []
    },
    {
      "name": "page",
      "type": "registry:ui",
      "title": "Page",
      "description": "The background everything else sits on: it sets the page colour, the text colour and a full-viewport min height, and nothing else in the kit does. Without it a card floats on browser-default white and quiet text is unreadable in dark mode. It adds no padding and no max width, because a page is not always a column of prose. Page is a div, so the real <body> behind it stays white - visible on a rubber-band scroll and behind any short page. Paint the body too: <html suppressHydrationWarning> and <body className=\"bg-page text-text\">. The suppressHydrationWarning is not optional either - the theme script sets attributes on <html> before React starts, and React reports that as a mismatch.",
      "files": [
        {
          "path": "src/components/page.tsx",
          "type": "registry:ui",
          "target": "components/page.tsx"
        }
      ],
      "registryDependencies": []
    },
    {
      "name": "surface",
      "type": "registry:ui",
      "title": "Surface",
      "description": "A box with no meaning: the card colour, a corner radius and padding, in three forms. Plain is the default and has neither border nor shadow; OutlineSurface draws a hairline edge in the border role; RaisedSurface lifts off the page with a small shadow. The forms are defined once here, so every other box in the kit - Card, ListItem - inherits them and nothing drifts. There is no PlainSurface, because Surface already is it. The shadow colour is keyed to the mode rather than to how light the page is, so raised also separates by its card colour and the shadow is a second signal, not the only one. Padding and radius are ordinary defaults - override them from outside with className when a surface holds an image to its own edges.",
      "files": [
        {
          "path": "src/components/surface.tsx",
          "type": "registry:ui",
          "target": "components/surface.tsx"
        }
      ],
      "registryDependencies": []
    }
  ]
}
